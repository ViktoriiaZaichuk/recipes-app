import Layout from "../../components/layout/Layout";

const Login = () => {

    return (
        <Layout>
            <main>
                <h1>Login form</h1>
            </main>
        </Layout>
    );
};

export default Login;